<!--
  Add this to the Wazuh agent configuration and mount the monitor log
  directory at /var/log/podman in the agent container.
-->
  <localfile>
    <log_format>json</log_format>
    <location>/var/log/podman/podman.json</location>
  </localfile>
